{
  "name": "receipt-organizer-monorepo",
  "private": true,
  "workspaces": [
    "receipt-dashboard"
  ],
  "scripts": {
    "build": "npm --prefix receipt-dashboard run build",
    "dev": "npm --prefix receipt-dashboard run dev",
    "preview": "npm --prefix receipt-dashboard run preview"
  }
}
